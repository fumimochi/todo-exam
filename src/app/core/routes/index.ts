export namespace AppData {
    export enum AppEnum {
        REGISTRATION = 'registration',
        AUTH = 'auth',
        PAGES = 'pages',
        PROFILE = 'profile',
        TODOS = 'todos',
        CATEGORIES = 'categories',
        USERS = 'users'
    }

    export enum Roles {
        ROOT_ADMIN = 'root-admin',
        USER = 'user'
    }
}