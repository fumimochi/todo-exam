export namespace AppData {
    export enum AppEnum {
        SIGN_IN = 'sign_in',
        SIGN_UP = 'sign_up',
        AUTH = 'auth',
        PAGES = 'pages',
        PROFILE = 'profile',
        TODOS = 'todos',
        CATEGORIES = 'categories',
        USERS = 'users',
    }   

    export enum Roles {
        ROOT_ADMIN = 'root-admin',
        USER = 'user'
    }
}