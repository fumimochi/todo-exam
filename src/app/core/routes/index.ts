export namespace AppData {
    export enum AppEnum {
        REGISTRATION = 'registration',
        AUTH = 'auth',
        LAYOUT = 'layout',
        PAGES = 'pages',
        PROFILE = 'profile',
        TODOS = 'todos',
        CATEGORIES = 'categories',
        DETAILS = 'details',
        USERS = 'users'
    }

    export enum Roles {
        ROOT_ADMIN = 'root-admin',
        USER = 'user'
    }
}